* add a test script that would, in a test home directory,
  flex most of the options of git-home-history and report
  errors.  to be run before make dist.
* add ghh give <dir_or_file> into <dir> [as of <timespec>]
    get the dir_or_file checked out to a directory
    check if directory exists.  if so tell user to move it,
    then checkout
